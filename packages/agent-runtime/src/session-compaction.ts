/**
 * Pi session.compact() — 自动压缩历史。
 *
 * 已迁移至 @lets-talk/infrastructure/session
 * 本文件保留用于向后兼容。
 */

export { maybeCompactSessionIfNeeded, shouldCompactSession } from "@lets-talk/infrastructure/session";