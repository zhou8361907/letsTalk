/**
 * 日志格式化（human-readable + pino msg）。
 *
 * 已迁移至 @lets-talk/infrastructure/format
 * 本文件保留用于向后兼容。
 */

export {
  formatDuration,
  formatStepMessage,
  formatPrettyLogLine,
  shouldUseJsonLog,
} from "@lets-talk/infrastructure/format";