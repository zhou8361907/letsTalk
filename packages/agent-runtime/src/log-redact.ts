/**
 * 日志内容截断与哈希。
 *
 * 已迁移至 @lets-talk/infrastructure/logging
 * 本文件保留用于向后兼容。
 */

export {
  hashText,
  truncateForProdLog,
  toWorkspaceRelativePath,
  safeEqualHash,
} from "@lets-talk/infrastructure/logging";