/**
 * 创建 Pi Agent 会话（最简版）
 *
 * 已迁移至 ./core/create-session.ts
 * 本文件保留用于向后兼容。
 */

export {
  createPiSession,
  type CreatePiSessionOptions,
  type PiSessionHandle,
} from "./core/create-session.js";