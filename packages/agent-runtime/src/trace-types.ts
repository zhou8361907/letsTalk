/**
 * Trace 类型定义。
 *
 * 已迁移至 @lets-talk/infrastructure/tracing
 * 本文件保留用于向后兼容。
 */

export type {
  TraceStepRecord,
  TurnTraceMeta,
  TraceRecord,
  TraceToolRecord,
  SessionLedgerEntry,
} from "@lets-talk/infrastructure/tracing";